/* Exam 2 has no separate "practice-exam" set — the entire verified bank
   (testbank.js, 335 Q mined from ~6 practice exams) IS the practice pool.
   PRACTICE is kept as an empty array so the engine's optional references
   (ALLQ concat, "practice" quiz mode) stay valid. */
const PRACTICE = [];
